---
- name: Disable SSH password authentication on development
  hosts: mtproto_dev
  become: true
  gather_facts: false
  pre_tasks:
    - name: Wait for stable SSH access
      ansible.builtin.wait_for_connection:
        timeout: 180
        connect_timeout: 10
        sleep: 5
  roles:
    - role: ssh_hardening
