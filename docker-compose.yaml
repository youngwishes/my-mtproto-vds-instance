services:
  telemt:
    image: whn0thacked/telemt-docker:latest
    container_name: telemt
    restart: unless-stopped
    environment:
      RUST_LOG: "info"
    volumes:
      - ./telemt.toml:/etc/telemt.toml:ro
    ports:
      - "443:443/tcp"  # Оставляем как есть - это публичный доступ
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    tmpfs:
      - /tmp:rw,nosuid,nodev,noexec,size=16m
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 256M
    depends_on:
      - api
    networks:
      - telemt-network  # Добавляем сеть

  api:
    build: .
    container_name: telemt-api
    # ВАЖНО: Меняем проброс порта - только на WireGuard IP
    ports:
      - "10.10.0.2:8000:8000"  # Теперь только через WireGuard
    volumes:
      - ./telemt.toml:/app/telemt.toml
      - ./src:/app/src/
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - telemt-network

# Создаем внутреннюю сеть для коммуникации между контейнерами
networks:
  telemt-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16  # Внутренняя сеть для контейнеров